/**
 *  Solucion de FUERZA BRUTA — se usa como oraculo en `cpx stress`.
 *  Debe ser obviamente correcta, aunque sea O(n!) o O(2^n).
 *  Lee exactamente el mismo formato de entrada que main.cpp.
 */
#include <bits/stdc++.h>
using namespace std;

using ll = long long;
#define all(x) (x).begin(), (x).end()
#define sz(x)  ((int)(x).size())

void solve() {
    // <<< version lenta pero correcta >>>
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int T = 1;
    // cin >> T;
    while (T--) solve();
    return 0;
}
